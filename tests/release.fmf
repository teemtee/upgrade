summary: Check the system release
test: |
    cat /etc/redhat-release

    if [[ "$IN_PLACE_UPGRADE" ==  "old" ]]; then
        grep -q 'Fedora release 43' /etc/redhat-release
    else
        grep -q 'Fedora release 44' /etc/redhat-release
    fi
